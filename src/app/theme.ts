// src/theme.ts
import { createTheme } from "@mui/material/styles";

export default createTheme({
  colorSchemes: {
    dark: true,
  },
  palette: {
    mode: "dark",
    primary: {
      main: "#0afffb",
    },
    secondary: {
      main: "#ffd780",
    },
  },
});
