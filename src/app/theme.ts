// src/theme.ts
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#f7b77e",
    },
    secondary: {
      main: "#0aece9",
    },
  },
});

export default theme;
